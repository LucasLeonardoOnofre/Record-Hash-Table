# Record-Hash-Table
A Java console application that stores and manages music records using a hash table with linear probing, folding hash function, and automatic rehashing for efficient lookup, add, and remove operations.
